// Snippet module: hero-page
export const section = `      <section id="hero-page">
        <h1>Hero (page-scoped)</h1>
        <p>
          A full-bleed page banner: a <code>figure &gt; img</code> plus a
          heading inside a page-level <code>header</code> rail.
        </p>
        <header class="fluid">
          <figure>
            <img
              src="https://picsum.photos/seed/jiffies-page/2000/500"
              alt="Decorative full-bleed page banner"
            />
            <h2>Page-scoped hero</h2>
          </figure>
        </header>
      </section>`;
